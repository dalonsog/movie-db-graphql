query GetUsers {
  users {
    id
    username
    role
    reviews {
      id
      title
      content
      stars
      movie {
        id
        title
        release
        director {
          id
          fullname
        }
      }
    }
  }
}